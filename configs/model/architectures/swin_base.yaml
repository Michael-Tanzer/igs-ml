# @package model.net
# Full net config for Swin Transformer Base (DataObjectModelWrapper + TimmClassifier).
_target_: src.models.components.model_wrapper.DataObjectModelWrapper
model:
  _target_: src.models.components.timm_classifier.TimmClassifier
  model_name: swin_base_patch4_window7_224
  in_chans: ???    # task-specific, set by model config (e.g. 3 * max_z)
  num_classes: ??? # task-specific, set by model config
  pretrained: false
  drop_rate: 0.2
  img_size: 224
dataobject_input_args:
  - data
