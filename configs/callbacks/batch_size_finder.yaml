batch_size_finder:
  _target_: src.models.callbacks.batch_size_finder.BatchSizeFinder
  mode: binsearch
  steps_per_trial: 3
  init_val: 64
  max_trials: 25
  batch_arg_name: batch_size_per_device
  margin: 0.20
